# 複製此檔為 .env 後填入（勿將 .env 提交到 Git）
# 專案 ID：登入 https://www.sanity.io/manage → 你的專案 → Project ID

# Astro 建置／前端讀取
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# Sanity Studio（npm run studio）建議一併設定，與上方相同即可
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Sanity 寫入 token（給 Astro API / 腳本用；Studio 請用瀏覽器登入，勿依賴 config 內嵌 token）
SANITY_STUDIO_TOKEN=
# SANITY_AUTH_TOKEN=
# SANITY_API_TOKEN=
# SANITY_WRITE_TOKEN=

# Studio 拖曳排序／儲存若出現 Request error：到 https://www.sanity.io/manage → 專案 → API → CORS origins
# 新增 http://localhost:3334（npm run studio）與 http://localhost:3333（npm run dev 的 /admin）
# 並在 Studio 右上角完成登入（Editor 以上權限）

# --- 選用：改為 Strapi（專案內 strapi-cms/，Sanity 仍保留）---
# PUBLIC_CMS=strapi
# 僅填「網站根網址」，勿加 /admin。程式會自行請求 {URL}/api/...；後台網址為 {URL}/admin
# 本機：http://localhost:1337　Strapi Cloud 例：https://你的專案.strapiapp.com
# PUBLIC_STRAPI_URL=http://localhost:1337
# Strapi 後台：cd strapi-cms && npm run develop（啟動時會自動為 Public 開 category/product/promotion 與
# Upload（content-api find/findOne），否則 populate 的圖片沒有 url）。僅手動勾選可設 STRAPI_SKIP_PUBLIC_CONTENT_PERMISSIONS=true
# Strapi 不自動建立三筆促銷時，在 strapi-cms/.env 設 STRAPI_SKIP_PROMOTION_SEED=true
# 不自動開 Upload「依 EXIF 旋轉」時：STRAPI_SKIP_UPLOAD_AUTO_ORIENTATION=true
# 略過「用 schema.json 的 config.metadatas 覆寫後台欄位中文標籤」時：STRAPI_SKIP_CM_LABEL_SYNC=true
