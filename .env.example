# 複製此檔為 .env 後填入（勿將 .env 提交到 Git）
# 專案 ID：登入 https://www.sanity.io/manage → 你的專案 → Project ID

# 官網前台網址（後台「前端／QR Code」按鈕用；未設則預設 https://boaquarium.vercel.app）
# PUBLIC_SITE_URL=https://boaquarium.vercel.app

# Astro 建置／前端讀取（Vercel 請在專案 Environment 設定；執行期優先讀 process.env，與 Studio／自製後台同一 dataset）
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# Sanity Studio（npm run studio）建議一併設定，與上方相同即可
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Sanity 寫入 token（給 Astro API / 腳本用；Studio 請用瀏覽器登入，勿依賴 config 內嵌 token）
SANITY_STUDIO_TOKEN=
# SANITY_AUTH_TOKEN=
# SANITY_API_TOKEN=
# SANITY_WRITE_TOKEN=

# Studio 拖曳排序／儲存若出現 Request error：到 https://www.sanity.io/manage → 專案 → API → CORS origins
# 新增 http://localhost:3334（npm run studio）與 http://localhost:3333（npm run dev 的 /cms-studio）
# 並在 Studio 右上角完成登入（Editor 以上權限）

# 官網與自製後台資料來源僅 Sanity（勿再設 PUBLIC_CMS／PUBLIC_ADMIN_BACKEND 走 Strapi）。

# --- 以下僅供本機維護 strapi-cms/ 子專案時參考；前台不讀 Strapi ---
# PUBLIC_STRAPI_URL=http://localhost:1337
